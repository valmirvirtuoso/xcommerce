import { styled } from "..";

export const BestSellersContainer = styled("aside", {

    color: "$gray900",

    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",

})